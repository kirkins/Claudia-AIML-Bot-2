var config = {};

// DyndamoDB databases; must enable access to script via amazon identity manager in aws console
config.msgDb = 'bot_msgs';
config.userDb = 'roberto_users';

// Microsoft Cogntitive Emotion API
config.MSemotionKey = '';

module.exports = config;
